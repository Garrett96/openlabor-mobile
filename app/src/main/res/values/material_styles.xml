<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Provide a fallback definition for the missing Material Components styles -->
    <style name="Widget.MaterialComponents.TextInputEditText" parent="Widget.AppCompat.EditText">
        <item name="android:padding">16dp</item>
        <item name="android:background">@android:color/transparent</item>
    </style>
    
    <!-- Style for TextInputLayout to ensure compatibility -->
    <style name="Widget.MaterialComponents.TextInputLayout.OutlinedBox" parent="Widget.Design.TextInputLayout">
        <item name="boxBackgroundMode">outline</item>
        <item name="boxBackgroundColor">@android:color/transparent</item>
        <item name="boxCornerRadiusTopStart">4dp</item>
        <item name="boxCornerRadiusTopEnd">4dp</item>
        <item name="boxCornerRadiusBottomStart">4dp</item>
        <item name="boxCornerRadiusBottomEnd">4dp</item>
    </style>
    
    <!-- Other potential missing material styles -->
    <style name="ThemeOverlay.MaterialComponents.Dialog.Alert" parent="ThemeOverlay.AppCompat.Dialog.Alert">
        <!-- Any needed attributes -->
    </style>
</resources>